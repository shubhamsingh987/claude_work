; Drop-in for homebridge.service. Install at:
;   /etc/systemd/system/homebridge.service.d/override.conf
;
; Already had Restart=always; same crash-loop-gives-up-permanently risk as
; mediamtx, just widening the tolerance window defensively.

[Unit]
StartLimitIntervalSec=300
StartLimitBurst=20
